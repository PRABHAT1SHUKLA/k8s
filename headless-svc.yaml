apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
spec:
  clusterIP: None  # This makes it headless (no single IP, just DNS)
  selector:
    app: nginx  # Matches Pods with this label
  ports:
    - port: 80  # Exposes port 80 (HTTP)
      name: web